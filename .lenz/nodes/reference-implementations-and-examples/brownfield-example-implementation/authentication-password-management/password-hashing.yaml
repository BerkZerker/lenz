kind: behavior
title: Password hashing
spec: Prefixes the given password string with "h:" and returns the resulting hashed string
  representation.
parent: n_db379b
status: proposed
examples:
  - id: ex_72b175
    name: Hash plaintext password
    given: A plaintext password string "secret123"
    when: hashPassword("secret123") is invoked
    then: It returns "h:secret123"
    expect:
      mode: manual
    derived: true
derived: true
anchors:
  - kind: function
    name: hashPassword
    container: ""
    file: examples/brownfield/src/auth/hash.ts
    sig: 377417802dc6
    body: 9cc543c21f8b
id: n_8b61f9
deps: []
summary: This behavior handles credential security by converting plaintext password strings into a
  formatted hashed representation. It operates as a standalone utility with no downstream handoffs,
  and it is invoked during credential updates by [[n_afe891]].
summary_at: 2026-08-27T03:49:09.974Z
